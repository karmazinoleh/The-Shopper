spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/the-shopper
    username: kalemz
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  mail:
    host: localhost
    port: 1025
    username: user
    password: user
    properties:
      mail:
        smtp:
          trust: "*"
        auth: true
        starttls:
          enable: true
        connectiontimeout: 3000
        timeout: 2000
        writetimeout: 3000
application:
  security:
    jwt:
      secret-key: A58C6C9B8EF2D2B523FCD494D7BCC
      expiration: 864000
  mailing:
    frontend:
      activation-url: http://localhost:4200/activate-account
server:
  port: 8088